{% extends 'base.html.twig' %}

{% block title %}
    Holiday Manager | {{ holidayUser }}'s Holidays
{% endblock %}

{% block body %}
    <div class="container">
        <div class="panel panel-primary">
            <div class="panel-heading clearfix">
                <a href="{{ path("listUsers") }}" class="btn btn-primary pull-left back-button"><i class="glyphicon glyphicon-arrow-left"></i></a>
                <h2 class="panel-title pull-left hm-panel-title">{{ holidayUser }}'s Holidays</h2>
                <a href="{{ path("selectHolidayForUser", {'username' : holidayUser }) }}" class="btn btn-primary pull-right"><i class="glyphicon glyphicon-plus"></i></a>
            </div>
            <div class="panel-body">
                {% if userHolidays %}
                    <table class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>Holiday Type</th>
                            <th>Year</th>
                            <th>Days</th>
                            <th class="text-center">Modify</th>
                            <th class="text-center">Delete</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for userHoliday in userHolidays %}
                            <tr>
                                <td>{{ userHoliday.getHolidayName() }}</td>
                                <td>{{ userHoliday.getYear() }}</td>
                                <td>{{ userHoliday.getDays() }}</td>
                                <td align="center"><a href="{{ path("updateUserHoliday", {'holidayId' : userHoliday.getHolidayId(), 'year' : userHoliday.getYear(), 'username' : holidayUser }) }}"><span class="glyphicon glyphicon-wrench"></span></a></td>
                                <td align="center"><a href="{{ path("deleteUserHoliday", {'holidayId' : userHoliday.getHolidayId(), 'year' : userHoliday.getYear(), 'username' : holidayUser }) }}"><span class="glyphicon glyphicon-minus"></span></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    <strong>Currently {{ holidayUser }} does not have any holiday!</strong>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}